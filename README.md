# html-docker-hello
Basic Publisching Html Page with Docker

# Run Command 1
docker image build -t dockerhtml .

# Run Command 2
docker container run -d --name dockerhtmlwebpage -p 80:80 dockerhtml

Go localhost and finish ;)

